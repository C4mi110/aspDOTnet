<!DOCTYPE html>
<html>
<head>

    <meta name="viewport" content="width=device-width">
    <link rel="stylesheet"
          asp-href-include="~wwwroot/lib/bootstrap/css/*.css"
          asp-href-exclude="**/*-reboot*,**/*-grid*" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>
        @if (TempData["messange"] != null)
        {
            <div>@TempData["messange"]</div>
        }
        @RenderBody()
    </div>
</body>
</html>